# Secrets — never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# macOS
.DS_Store

# Intermediate pipeline output — regenerated by main.py
temp/

# Research outputs — kept local (may quote copyrighted primary texts)
results/

# Corpus — supplied locally per `data/README.md`; not redistributed
data/plaintext/
data/meta.csv

# Debug artifacts
data/meta_poetry_only.csv
